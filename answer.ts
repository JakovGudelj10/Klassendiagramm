namespace QuizApp {

    export class Answer {

        private _answer: string;
        private _isBoolean: boolean;

        constructor(_answer: string, _isBoolean: boolean) {

            this._answer = _answer;
            this._isBoolean = _isBoolean;
        }

        public showSolution(boolean): void {

        }

    }
}
